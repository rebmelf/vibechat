# vibechat
Chatbot with a twist!

## Current setup
- Create a .env file in the project folder
- Add the following env variables:
  - OPENAI_API_KEY
  - PINECONE_API_KEY
- run the app with `chainlit run main.py`
